# Storyteller configuration
# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit it.

# ---------------------------------------------------------------------------
# Data pool — directory storyteller scans for raw files
# Set this so you can run `storyteller ingest` without specifying a path.
# ---------------------------------------------------------------------------
STORYTELLER_DATA_DIR=data/

# Output directory for generated JSON and markdown files (default: output/)
STORYTELLER_OUTPUT_DIR=output/

# Path to the ingested JSON file consumed by `timeline` and `prepare`
# (default: <STORYTELLER_OUTPUT_DIR>/ingested.json)
# STORYTELLER_INGESTED_FILE=output/ingested.json

# ---------------------------------------------------------------------------
# GitHub publishing
# Requires `gh` CLI to be installed and authenticated (`gh auth login`).
# ---------------------------------------------------------------------------

# Publishing mode: "gist" (default) or "repo"
STORYTELLER_GITHUB_PUBLISH_MODE=gist

# Make gists public (default: false — secret gists)
# STORYTELLER_GITHUB_GIST_PUBLIC=false

# Target repository in "owner/repo" form (required for mode "repo")
# STORYTELLER_GITHUB_REPO=your-username/your-repo

# Directory inside the repo where summaries are committed (mode "repo")
# STORYTELLER_GITHUB_REPO_PATH=summaries/
